# 인스턴스(instance)
# 자료형이 객체(object)화 된 상태 인스턴스
# 객체를 생성할 떄 메모리가 할당 될때 인스턴스
# 객체: 고유한 자료의 실체를 구별

# type : 자료형을 알려 준다.
f = 0.1234
print('type:', type(f)) # float(실수형)

fs = str(type(f))
print('type:', fs)

